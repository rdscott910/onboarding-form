/**
 * This file exports all constants used throughout the application.
 *
 *   - Centralized validation - You can add type checking and default values in one place
 *   - Better error handling - Catch missing env variables during initialization
 *   - Improved maintainability - Single source of truth for all env variables
 *   - Clear categorization - Logical grouping makes it easier to find and update related constants
 *   - TypeScript support - Better type inference and autocomplete
 *   - Clean imports - Consistent import pattern across the codebase
 *
 * The `email` constants are currently the only constants exported.
 */

export * from './email';
